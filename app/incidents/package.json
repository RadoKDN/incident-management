
{
  "name": "incidents-ui",
  "version": "1.0.0",
  "description": "Fiori Elements UI for Incident Management",
  "scripts": {
    "build:cf": "ui5 build --config ui5-deploy.yaml --include-task=generateManifestBundle --include-task=generateCachebusterInfo"
  },
  "devDependencies": {
    "@ui5/cli": "^4.0.50",
    "@sap/ux-ui5-tooling": "1",
    "ui5-task-zipper": "^3.6.0"
  }
}
